/**
 * Component whose source declares a do/never block that cannot be parsed
 * into a full rule: the DO line carries no text.
 *
 * @usage-patterns
 * DO:
 */
export const misruleBaseClasses = 'flex';
