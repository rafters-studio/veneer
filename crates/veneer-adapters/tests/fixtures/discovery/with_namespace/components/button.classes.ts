export const buttonBaseClasses = 'inline-flex items-center justify-center';
export const buttonVariantClasses: Record<string, string> = {
  default: 'bg-primary text-white',
  secondary: 'bg-secondary text-black',
};
